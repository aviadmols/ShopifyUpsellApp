# Config as Code – overrides dashboard settings
# https://docs.railway.app/config-as-code

[build]
builder = "DOCKERFILE"

[deploy]
# Must run our script; otherwise Railway may run a different command (e.g. one that prints APP_KEY and exits).
startCommand = "/docker-start.sh"
healthcheckPath = "/up"
healthcheckTimeout = 30
